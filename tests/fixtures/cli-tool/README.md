# cli-tool

A `clap` derive surface: a program with a name and an about, subcommands with
doc comments, positional arguments with defaults, flags, options with a value
name, and one command that takes nothing at all.
