# go-handler-collision

Two functions named `MakeHourAvailable` in one package: a gRPC method in
`grpc.go` and the HTTP handler in `http.go`. The shape from wild-workouts, which
had four of them. `grpc.go` sorts first, so resolving a route by bare name cited
the gRPC method for an HTTP route.
