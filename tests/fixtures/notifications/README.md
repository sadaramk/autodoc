# notifications

The service the `cross-repo` fixture calls. It exists as a separate fixture
directory on purpose: a member of a system is another repository, and the point
of the multi-repository tests is that neither book is complete without the
other.
