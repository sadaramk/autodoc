services:
  db:
    image: postgres:17

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      DATABASE_URL: postgresql://app@db:5432/app
    depends_on:
      - db

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
