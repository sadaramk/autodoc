# Mostly Unread

A trading platform whose matching engine is C++ and whose mobile client is
Dart. Only the Go gateway is in a language nunki reads, so a book about this
repository is a book about a fraction of it — and has to say so.
