# quarkus-panache

Quarkus Hibernate ORM with Panache (Gradle): active-record `Device extends PanacheEntity` with an implicit id, static
finders, `persist()`, a `PanacheRepository`, and an enum lifecycle changed by field assignment in a JAX-RS resource.
