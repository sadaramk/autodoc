# liquibase

Liquibase changelogs: a YAML master including an XML and a YAML changelog (createTable, renameColumn,
addForeignKeyConstraint), merged with a plain Hibernate entity (no Spring naming) and accessed through `EntityManager`.
