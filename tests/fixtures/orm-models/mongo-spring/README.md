# mongo-spring

Spring Data MongoDB carts: `@Document(collection)`, default collection naming, `@Field` wire names, `@Indexed(unique = true)`,
`@DBRef`, and access through a `MongoRepository` and a `MongoTemplate`.
