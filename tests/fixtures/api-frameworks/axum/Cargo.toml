[package]
name = "accounts"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
validator = { version = "0.18", features = ["derive"] }
